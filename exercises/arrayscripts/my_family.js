"use strict";

let myFamily = ["Traci", "Sylonjia", "Zion", "..." ];
// for loop
for (let i = 0; i < myFamily.length; i++) {
    const famElement = (myFamily[i]);
    console.log(famElement);
}

/**while loop**
let loop = 0;
while ( loop <= myFamily.length ) {
    const famElement = myFamily[loop];
    console.log(famElement);
  loop++; }
  */