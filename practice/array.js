//create data
let kids = ["Natalie", "Brittany", "Zachary"];
//find data
for (let i = 0; i < kids.length; i++) {
    const element = kids[i];
    console.log(element);}

    //This formula makes sure that if more data is added, the loop just cycles through with jams as apposed to hard coding.